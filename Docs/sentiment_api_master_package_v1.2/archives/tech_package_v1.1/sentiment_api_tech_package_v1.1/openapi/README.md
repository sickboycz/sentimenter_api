# OpenAPI

- `sentiment_api.openapi.yaml` is the authoritative REST contract for v1.0.
- It references JSON Schemas under `../schemas/`.

Use this file to:
- generate client SDKs,
- scaffold FastAPI routes,
- validate request/response payloads.
