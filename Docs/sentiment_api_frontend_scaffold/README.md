# sentiment_api_stack (frontend included)

This repo skeleton contains:

- `frontend/` — Next.js dashboard (glassy shell).
- `docker-compose.yml` — local dev stack (frontend + api + observability).
- `docker/frontend/Dockerfile` — builds the dashboard container.

See `frontend/README.md` for dashboard run commands.
